# =============================================================================
# Docker Compose - Tencent Test Environment
# =============================================================================
# Usage:
#   docker-compose -f docker-compose-tencent_test.yml up -d    # Start
#   docker-compose -f docker-compose-tencent_test.yml logs -f  # View logs
#   docker-compose -f docker-compose-tencent_test.yml down      # Stop
# =============================================================================

services:
  payroll-test:
    container_name: payroll-test
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_APP_MODE=test
    ports:
      - "8001:8000"
    env_file:
      - ~/payroll/env_docker_remote_mysql_test.sh
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
      - LOG_DIR=/app/logs
      - CLOUDBASE_RUN=false
      - DOMAIN_NAME=localhost
      - BACKEND_PORT=8000
      - FRONTEND_PATH=/app/frontend/dist
      - API_ENDPOINT=http://127.0.0.1:8000
      - ENV=test
    volumes:
      - ./logs:/app/logs:rw
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health', timeout=2)"]
      interval: 30s
      timeout: 3s
      start_period: 10s
      retries: 3
